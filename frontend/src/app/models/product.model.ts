export interface Product {
  productId: number;
  productName: string;
  productDescription: string;
  brandName: string;
  variantPrice: number;
  variantSku: string;
  // We can add more fields here later, like imageUrl.
}
